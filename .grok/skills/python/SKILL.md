# Python

Stack outillage : Python 3 pour les **projets** forgés, pas pour le menu (le menu est bash).

Projets forgés : Python ≥ 3.12, `pyproject.toml`, paquets sous `src/`. Run via `./install.sh` du **projet**, pas un pip global. Ubuntu PEP 668 : jamais `pip install --user` sur le Python système.

Ne pas toucher `frontend/` ni `package.json` (skill npm).
