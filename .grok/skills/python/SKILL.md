# Python

Stack outillage : Python 3, TUI `textual` (installé au besoin par `forge_tui.py`).

Projets forgés : Python ≥ 3.12, `pyproject.toml`, paquets sous `src/`. Run via `./install.sh` du **projet**, pas un pip global.

Ne pas toucher `frontend/` ni `package.json` (skill npm).
