# Linux

Cible : Ubuntu-like, bash `set -euo pipefail`.

`install.sh` (ce dépôt) : paquets apt, gh, grok, uv, fnm. Lectures via `/dev/tty`.

Idempotent : relancer ne casse pas un état déjà bon. Pas de secret en dur.
