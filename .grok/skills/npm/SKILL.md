# npm

JS/TS des **projets** forgés. Fullstack : frontend sous `frontend/` si le socle l’a posé.

`./install.sh` du projet fait `npm install`. Ne pas toucher `.venv/` ni `pyproject.toml` (skill python).
