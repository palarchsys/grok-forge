# Session

Projet : __NAME__. Point d’entrée unique : `./install.sh`.

## Démarrage

```bash
chmod +x install.sh && ./install.sh
```

Ne pas inventer une autre séquence (venv + pip + npm + exports).

## Pendant la session

1. Table de routage dans `AGENTS.md` → un skill.
2. Modifier uniquement les cibles.
3. Tests du socle s’ils existent.
4. Commit + push selon `.grok/skills/git/SKILL.md`.

## Fin

Note dans `docs/reports/` si le changement n’est pas trivial. Pas de roman.
